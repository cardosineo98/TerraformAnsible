---
- hosts: all
  handlers:
    - name: restart apache
      service: 
        name: apache2
        state: restarted
      become: yes
  tasks:
<<<<<<< HEAD
  - name: Instalando o python3, vertualenv
    apt: 
     pkg:
      - python3
      - virtualenv
     update_cache: yes
    become: yes
  - name: Instalando Dependencias PIP (Django e DJango rest)
    pip: 
     virtualenv: /home/ubuntu/tcc/venv
     name:
      - django
      - djangorestframework
  - name: Iniciando Projeto
    shell: '. /home/ubuntu/tcc/venv/bin/activate; django-admin startproject setup /home/ubuntu/tcc/'
  
  - name: Alterando Host do settings
    lineinfile:
     path: /home/ubuntu/tcc/setup/settings.py
     regexp: '^ALLOWED_HOSTS ='
     line: "ALLOWED_HOSTS = ['*']"
     backrefs: yes
      
=======
    - name: Install dependencys
      ansible.builtin.apt:
        pkg:
        - apache2
        - ghostscript
        - libapache2-mod-php
        - mysql-server
        - php
        - php-bcmath
        - php-curl
        - php-imagick
        - php-intl
        - php-json
        - php-mbstring
        - php-mysql
        - php-xml
        - php-zip
        state: latest
        update_cache: yes
      become: yes
    - name: Criando Diretorio
      ansible.builtin.file:
        path: /srv/www
        state: directory 
        owner: www-data
        group: www-data
      become: yes
    - name: download and extract
      ansible.builtin.unarchive:
        src: https://wordpress.org/latest.tar.gz
        dest: /srv/www
        remote_src: yes
      become: yes
    - name: Copy file with owner and permissions
      ansible.builtin.copy:
        src: files/wordpress.conf
        dest: /etc/apache2/sites-available/000-default.conf
      become: yes
      notify:
        - restart apache
        
>>>>>>> 1018f22 (Instalacao servidor wordpress 1 etapa)
